---
- name: 'deploy common configurations'
  hosts: 'all'
  vars:
    install_packages: false
    vim_plugins:
      - 'nerdtree'
      - 'syntastic'
      - 'fugitive'
      - 'mru'
      - 'rust'
      - 'rustdoc'
      - 'commandt'
      - 'youcompleteme'
  roles:
    - 'bash'
    - 'vim'
    - 'viewnior'
